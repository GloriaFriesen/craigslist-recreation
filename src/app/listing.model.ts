export class Listing {
  constructor (public item: string, public price: number, public category: string, public image: string, public description: string, public seller: string, public id: number) { }
}
